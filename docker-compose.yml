version: '3.8'

services:
  sneaky-snake:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
    command: python3 -m flask run --host=0.0.0.0
